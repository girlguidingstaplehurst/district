## Implementation

- [x] Add Git/release version resolution with a safe development fallback.
- [x] Add the frontend build wrapper that injects the resolved version.
- [x] Update npm build configuration to use the wrapper.
- [x] Render the version in the shared footer.
- [x] Update the release workflow to install Node, install dependencies, and build the frontend with the release tag.

## Verification

- [x] Add footer and version-resolution tests.
- [x] Run the frontend test suite.
- [x] Run the production frontend build.
- [x] Check the resulting changes for whitespace errors.
